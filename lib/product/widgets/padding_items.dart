import 'package:flutter/material.dart';

class PaddingItems {
  static const EdgeInsets symmetricPadding = EdgeInsets.symmetric(
    horizontal: 17,
    vertical: 12,
  );
  static const EdgeInsets topPadding = EdgeInsets.only(top: 16);
  static const EdgeInsets onlyPadding = EdgeInsets.only(top: 16, bottom: 16);
  static const EdgeInsets verticalPadding = EdgeInsets.symmetric(vertical: 12);
  static const EdgeInsets allPadding = EdgeInsets.all(20);
  static const EdgeInsets smallAllPadding = EdgeInsets.all(4);
  static const EdgeInsets bottomPadding = EdgeInsets.only(bottom: 100);
  static const EdgeInsets bottomPadding12 = EdgeInsets.only(bottom: 12);
  static const EdgeInsets bottomPadding30 = EdgeInsets.only(bottom: 30);
  static const EdgeInsets bigverticalPadding = EdgeInsets.symmetric(
    vertical: 250,
  );
  static const horizantalPadding = EdgeInsets.symmetric(horizontal: 6);
  static const rightPadding = EdgeInsets.only(right: 12);
}
